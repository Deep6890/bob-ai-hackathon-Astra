# AeroReady Frontend — Environment Variables
# Copy this file to .env (same directory) before running npm run dev.

# Backend API base URL — change if your backend runs on a different port or host
VITE_API_URL=http://127.0.0.1:5000/api/v1
